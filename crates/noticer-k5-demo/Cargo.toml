[package]
name = "noticer-k5-demo"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Public Tier A end-to-end inspector for AEPA provenance"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

