name: quotient-accountant-core

on:
  push:
    paths:
      - "crates/quotient-accountant-core/**"
      - ".github/workflows/quotient-accountant-core.yml"
  pull_request:
    paths:
      - "crates/quotient-accountant-core/**"
      - ".github/workflows/quotient-accountant-core.yml"

jobs:
  conditional-composition:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.85.0
        with:
          components: rustfmt, clippy
      - run: cargo fmt --manifest-path crates/quotient-accountant-core/Cargo.toml --check
      - run: cargo clippy --manifest-path crates/quotient-accountant-core/Cargo.toml --all-targets -- -D warnings
      - run: cargo test --manifest-path crates/quotient-accountant-core/Cargo.toml
