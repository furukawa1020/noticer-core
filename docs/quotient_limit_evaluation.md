# QuotientLimit Scalability, Ablation, and Gap Evaluation

K9-QL-14 freezes the full 9,216-point Cartesian matrix over private histories, quotient classes, horizon, services, observer projections, and fault scenarios. It also freezes eight paired comparisons, yielding sixteen named ablation configurations.

Completed observations must report variables, equalities, inequalities, nonzeros, solver time, exact reconstruction time, checker time, peak memory, certificate size, maximum integer bit length, mutation rejection, and cross-platform byte reproducibility. Failed observations cannot masquerade as partial completions.

Optimality gaps use exact fractions. Reports preserve missing cells, prohibit interpolation, bind deterministic case identities, and mark generated artifacts as uncommitted. The protocol does not fabricate performance results; executors populate it in later measured runs.
